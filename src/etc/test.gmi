# hello world

whee
```
$$$ diagon Tree
aa
 bb
 cc
  dd
 dd
$$$
```

generic test flowchart 
```
$$$ diagon Flowchart
if ("pogger?") {
  ":)";
} else {
  ":(";
}
$$$
```

a table
```
$$$ diagon Table
hello, world
a, b
c, d
$$$
```

some shell commands
```
$$$ sh
set -- a shell script is making this, little sh boi. just shitty example to show you could proper script in
len="$#"
for a in $(seq 1 "$len"); do
	for b in $(seq 1 "$a"); do
		printf '#'
	done
	printf '\\'
	for b in $(seq 1 "$((len-$a))"); do
		printf '.'
	done
	printf ' %s\n' "$1"
	shift
done
$$$
```
